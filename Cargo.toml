[package]
name = "mc"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", default-features = false, features = [
    "std",
    "help",
    "usage",
    "error-context",
    "suggestions",
    "derive",
] }

[profile.release]
strip = true
lto = true
